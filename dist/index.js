'use strict'; // This is the entry file for the whole project.
// When executed it boots up the game server.
if(process.env.NODE_ENV==='production'){require('newrelic')}require('./server/bootstrap');
//# sourceMappingURL=index.js.map